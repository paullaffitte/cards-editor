# semver config file -- https://github.com/paullaffitte/semver

version: 1.2.1
files:
  README.md: 'latest \([^\(]*\)'
  package.json: '"version": "[^"]*",'
  example/deck.json: '"version": "[^"]*",'
  src/state/initialState.js: "version: '[^\"]*'"
